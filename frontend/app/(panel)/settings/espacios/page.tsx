import { SpacesManager } from "@/components/spaces/spaces-manager"

export const metadata = { title: "Espacios — Configuración" }

export default function EspaciosPage() {
  return <SpacesManager />
}
