import { EInvoiceManager } from "@/components/settings/einvoice-manager"

export const metadata = { title: "Facturación Electrónica — Configuración" }

export default function FacturacionElectronicaPage() {
  return <EInvoiceManager />
}
