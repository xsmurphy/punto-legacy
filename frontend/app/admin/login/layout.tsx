/**
 * Layout standalone del login admin. Sin sidebar, sin guard.
 * La page implementa su propio UI centrado.
 */
export default function AdminLoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
