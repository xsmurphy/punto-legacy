# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# service worker compilado por @serwist/next desde app/sw.ts (next.config.ts)
# — se regenera en cada build, no debe versionarse
/public/sw.js
# worker auxiliar de `cacheOnNavigation` (hoy apagado). También es output del
# build: estuvo versionado por accidente hasta 2026-08-23.
/public/swe-worker-*.js

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# typescript
*.tsbuildinfo
next-env.d.ts
