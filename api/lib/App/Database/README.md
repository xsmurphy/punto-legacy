# `Punto\App\Database`

Capa de acceso a BD: `Query::execute()`, `Query::insert()`, etc. Reemplaza `ncmExecute`, `ncmInsert`, `ncmUpdate`, `getValue`.

Slice que llena este directorio: 10 (Query). Crítico — 127 callers.
