# graphify — paths relative to the folder you run `/graphify` on
# https://github.com/safishamsi/graphify

# Composer / deps
**/vendor/
**/node_modules/

# Large vendored or generated trees
panel/libraries/adodb/
app/libraries/adodb/
**/php-thumb/

# Cache & env
.env
*.log
mysql_data/
postgres_data/

# Graphify's own outputs (avoid recursive ingestion)
graphify-out/

# Vendor libs (already analyzed; libs don't add project knowledge)
assets/vendor/
node_modules/

# UI assets (backgrounds, icons — no semantic value)
assets/images/
app/images/
panel/images/

# Build artifacts
.build_tmp/
app/cach/

# Optional: skip huge UI bundles if graphify feels slow
# panel/screens/mapIframe.php
